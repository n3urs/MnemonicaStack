import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oscarsullivan.mnemonica',
  appName: 'Mnemonica Trainer',
  webDir: 'dist'
};

export default config;
